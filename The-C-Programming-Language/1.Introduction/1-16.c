#include <stdio.h>

int get_line(char line[], int max_line);
void copy(char to[], char from[]);


int main(void) {
	int max = 0;
	int max_line = 1000;
	char line[max_line];
	char longest[max_line];
}
